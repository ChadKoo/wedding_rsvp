const rsvpBtn = document.getElementById('rsvpBtn');
const attendeesList = document.getElementById('attendees');
const firstNameInput = document.querySelector('input[placeholder="First Name, Last Name"]');
const addressInput = document.querySelector('input[placeholder="Address"]');
const phoneInput = document.querySelector('input[placeholder="Phone Number"]');